## 1. Cài đặt thư viện
```
pip install -r requirements.txt
pip install -r requirements.txt --upgrade
```
## 2. Kiểm tra version chrome
chrome://version/

# TODO
- hiên thị các bước lên giao diện
- chạy từng bước đồng thời hiển thị trạng thái của các bước đó (thành công, thất bại, đang chạy)
- thêm chức năng cấu hình các bước đó sau đó lưu vào file json
- thêm chức năng cấu hình để lưu 1 vài cấu hình chung như khung giao diện (dài rộng)
- nâng cao (chạy nhiều chrome và cho phép gán cấu hình proxy cho từng chrome đó)